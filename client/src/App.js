import React from 'react';

//get the hooks working first then set up routing

//as far as routing goes start with the login page and create-account page

//in the query hook i will get two parameters back run, results of query
//maybe a better idea is to just put the hook where i want it to run and just return data,
//maybe return a loading property later

const App = () => {
  return (
    <>
      <h1>hello world</h1>
    </>
  );
};

export default App;
